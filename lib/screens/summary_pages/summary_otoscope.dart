import 'package:flutter/material.dart';

class SummaryOtoscope extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Otoscope"),
    );
  }
}
